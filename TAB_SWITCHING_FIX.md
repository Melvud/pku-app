# Исправление задержек при переключении вкладок

## Проблема
При переключении между вкладками (Дневник, Статьи, Рецепты, Админ панель) происходила задержка в виде загрузки данных. Даже после добавления локального хранилища загрузка данных происходила при каждом переключении вкладки.

## Причина
Использование `_screens[_currentIndex]` в `MainNavigationScreen` приводило к тому, что:
1. При каждом переключении вкладки все виджеты экранов пересоздавались
2. Метод `initState()` вызывался повторно для каждого экрана
3. Происходила повторная загрузка данных (даже из кеша), что вызывало показ индикатора загрузки
4. Терялось состояние экранов (позиция прокрутки, введенный текст поиска и т.д.)

## Решение

### 1. Использование IndexedStack
Вместо отображения только текущего экрана через `_screens[_currentIndex]`, теперь используется `IndexedStack`:

```dart
body: IndexedStack(
  index: _currentIndex,
  children: _screens,
)
```

**Преимущества IndexedStack:**
- Все экраны остаются в памяти и сохраняют свое состояние
- `initState()` вызывается только один раз для каждого экрана
- Мгновенное переключение между вкладками
- Сохранение позиции прокрутки и других состояний UI

### 2. Оптимизация загрузки данных
Добавлены проверки в методы `initState()` всех экранов для предотвращения повторной загрузки уже загруженных данных:

#### ArticlesScreen
```dart
if (adminProvider.articles.isEmpty) {
  adminProvider.loadArticles();
}
```

#### AdminPanelScreen
```dart
if (adminProvider.appStats.isEmpty) {
  adminProvider.loadAppStats();
}
if (adminProvider.pendingRecipes.isEmpty) {
  adminProvider.loadPendingRecipes();
}
// и т.д.
```

#### RecipesScreen
```dart
if (provider.recipes.isEmpty) {
  provider.loadApprovedRecipes();
}
if (provider.myRecipes.isEmpty) {
  provider.loadMyRecipes();
}
```

#### HomeScreen
```dart
// Проверка наличия профиля и записей дневника
if (hasProfile && hasEntries) {
  setState(() => _isLoading = false);
  return;
}
```

## Результат

### До исправления:
- ⏱️ Задержка 0.5-2 секунды при переключении вкладок
- 🔄 Показ индикатора загрузки при каждом переключении
- 📜 Сброс позиции прокрутки
- 🔁 Повторная загрузка данных из кеша

### После исправления:
- ⚡ Мгновенное переключение между вкладками
- ✅ Нет индикаторов загрузки при переключении
- 💾 Сохранение состояния экранов (позиция прокрутки, текст поиска)
- 🎯 Данные загружаются только один раз при первом открытии
- 🚀 Плавный UX без мерцаний

## Измененные файлы
1. `lib/main.dart` - использование IndexedStack
2. `lib/screens/articles/articles_screen.dart` - оптимизация загрузки
3. `lib/screens/admin/admin_panel_screen.dart` - оптимизация загрузки
4. `lib/screens/recipes/recipes_screen.dart` - оптимизация загрузки
5. `lib/screens/home/home_screen.dart` - оптимизация загрузки

## Тестирование
Для проверки исправления:
1. Запустите приложение
2. Войдите в систему
3. Переключайтесь между вкладками: Статьи → Дневник → Рецепты → Настройки → Админ
4. Убедитесь, что переключение происходит мгновенно без индикаторов загрузки
5. Прокрутите список в любой вкладке, переключитесь на другую вкладку и вернитесь - позиция прокрутки должна сохраниться

## Дополнительные преимущества
- 🔋 Снижение нагрузки на процессор (меньше пересозданий виджетов)
- 💰 Экономия Firebase reads (данные загружаются один раз)
- 📱 Улучшение отзывчивости UI
- 🎨 Консистентный UX во всем приложении
